class PersistenceManager {
  preprocess(j) {
    //
  }

  saveToFile(journal, filename) {
    console.log("Saving...");
  }

  loadFile(filename) {
    //
  }

  loadFileFromUrl(url) {
    //
  }
}

module.exports = PersistenceManager;
